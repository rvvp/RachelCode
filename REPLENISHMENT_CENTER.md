# 智能补货中心

这是与“藏宝阁”完全独立的销售库存监控和补货协作服务。当前按店铺建立相互隔离的工作区：

- 马天奴唯品会：已运行的首期店铺
- 马天奴天猫官方旗舰店：天猫 API 联调工作区
- BNX唯品会：使用唯品会 VOP API 的下一家单店闭环试跑店铺
- 默认计划频率：每周二、周五 10:00
- 主流程：数据同步或导入 → 自动建议 → 商品部修正 → 跟单部确认 → 导出及留档

## 当前试跑能力

- 商品部销售库存监控台
- 未来 7 天、14 天缺货和核心尺码断码预警
- 30、45、60 天目标覆盖参数
- 7/14 日均加权、尺码曲线平滑、在途和装箱倍数计算
- 按货号筛选：条件1（近7天销量不低于5件且近14天不低于10件）或条件2（连续3天有销量）；两者共同要求库存支撑不超过14天
- 补货计划按“款号 → 货号/颜色 → 尺码”分层展示
- 周二、周五自动生成，页面可修改星期和时间
- 商品部逐尺码修正，修改时强制记录原因
- 跟单部填写可供数量、下单日期、到货日期和供应状态
- 独立 Excel 导入模板及补货计划导出
- 站内待办和登录后弹出提醒
- 独立账号、数据库和访问入口

唯品会 VOP 官方接口已经接入配置和试连边界。没有有效商家凭证时，系统只探测官方网关，不会伪造同步成功；Excel 仍是正式授权完成前的应急入口。

同时提供“浏览器报表采集”试跑入口，用于 API 申请完成前接收唯品 VIS 和魔方罗盘官方后台导出的报表。该方式使用隔离的 Chrome 配置目录，只复用人工登录后的会话，不读取或保存唯品账号密码，也不会绕过验证码、短信验证、设备验证或平台风控。

当前使用的官方服务：

- 生产网关：`https://vop.vipapis.com`
- 沙箱网关：`http://sandbox.vipapis.com`
- 店铺验证：`vipapis.marketplace.store.StoreInfoService#getStoreInfo`
- 商品映射：`vipapis.marketplace.product.ProductService#getProducts/getProductById`
- 订单销售：`vipapis.marketplace.delivery.SovDeliveryService#getOrders/getOrderDetail`
- SKU 库存：`vipapis.marketplace.inventory.InventoryService#getSkuStock`

## 启动

```bash
./scripts/start_replenishment.sh --host 0.0.0.0 --port 8877
```

浏览器访问 `http://127.0.0.1:8877`。

如需登录系统后自动启动并在异常退出时自动恢复，可安装本机服务：

```bash
./scripts/install_replenishment_launchd.sh
```

安装程序会将运行副本首次部署到 `~/ReplenishmentCenterDeploy`，避免 macOS 后台服务受到“文稿”目录隐私权限限制。重复安装会更新程序代码，但不会覆盖部署目录中的 `replenishment_data/` 业务数据。

卸载本机服务：

```bash
./scripts/uninstall_replenishment_launchd.sh
```

试跑账号：

- 商品部：`merch / demo123`
- 跟单部：`followup / demo123`
- 管理层：`manager / demo123`
- 管理员：`admin / demo123`

默认独立数据库为：

```text
replenishment_data/replenishment.db
```

可通过参数或环境变量修改：

- `REPLENISH_HOST`
- `REPLENISH_PORT`
- `REPLENISH_DB`
- `REPLENISH_SEED_DEMO`
- `REPLENISH_SCHEDULER`

## 数据模板

登录商品部账号后，从“数据中心”下载模板。工作簿包含：

- `SKU资料`
- `销售数据`
- `库存及在途`

上传时会校验工作表、表头、日期、数字以及销售/库存 SKU 是否已经在 SKU 资料中定义。相同 SKU 和日期会更新，不会重复累计。

## 唯品浏览器报表采集

使用商品部或管理员账号登录后，进入：

```text
数据中心 → 浏览器报表采集
```

首次试跑顺序：

1. 点击“打开唯品后台登录”，在新打开的专用 Chrome 中自行完成唯品登录和安全验证。
2. 进入“魔方罗盘 → 货品运营 → 商品明细”，选择马天奴、最近完整 14 天、分天查看和条码粒度。
3. 将商品明细页同时记录为销售和库存报表页，分别开始任务后导出一份联合报表。
4. 进入“VIS → 商品管理 → 商品资料”，记录为商品主数据页，并导出“商品基础信息”。
5. 下载完成后系统每 30 秒扫描一次，也可点击“扫描新下载文件”。
6. 三类记录齐全后点击“关联报表并生成计划”。

原始报表保存在 `replenishment_data/vipshop_raw_reports/日期/`，并记录 SHA-256、文件大小、表头和行数。商品明细提供日期、款号、条码、尺码、销售量和在售库存；商品基础信息通过条码补齐标准/自定义颜色和尺码。

导入前会校验品牌、14 个统计日期、重复条码以及商品主数据匹配率。条码未匹配比例超过 5% 时整批拒绝，不会覆盖现有数据；成功后同一组报表会标记为已导入，不能重复生成计划。商品主数据可复用，只有新增商品或颜色尺码资料变化时才需更新。

补货候选先按货号汇总全部尺码的销量、可售库存和有效在途。货号满足以下任一销量条件即可：条件1为近7天销量不低于5件且近14天不低于10件；条件2为最近14个完整自然日内存在任意连续3天、每天汇总销量均大于0。两种条件都必须同时满足库存支撑不超过14天的共同门槛。货号进入计划后，再按近14天尺码销量占比和默认尺码曲线拆分 30/45/60 天建议量。筛选门槛可在“频率设置”中修改，并会冻结在每个历史计划中。

浏览器取数模式的定时任务会在设定时间使用已登录的唯品专用 Chrome，自动选择前14个完整自然日、马天奴品牌、分天查看、跨天不去重和条码粒度，等待唯品下载中心生成完成后接收官方报表。报表通过日期、字段、品牌和条码匹配校验后，才更新销售库存并生成新计划。登录失效、安全验证或导出失败时，系统记录“更新未完成”并提醒商品部，不会使用旧销售和库存生成新的补货计划。

## 唯品会 API 配置

使用商品部或管理员账号登录后，进入：

```text
数据中心 → API 配置与试连
```

需要填写：

- `AppKey`
- `AppSecret`
- 店铺主账号授权得到的 `AccessToken`
- 预期店铺名称，第一期保持为“马天奴”

`AppSecret` 和 `AccessToken` 会用本机独立密钥加密后写入数据库；本机密钥保存在 `replenishment_data/replenishment.key`，权限为 `0600`，不会进入源码提交。也可以完全不在页面保存凭证，改用环境变量：

- `VIPSHOP_ENVIRONMENT=production`
- `VIPSHOP_APP_KEY`
- `VIPSHOP_APP_SECRET`
- `VIPSHOP_ACCESS_TOKEN`

配置后先执行“测试网关与店铺鉴权”。系统会调用只读店铺接口，并检查授权店铺名称包含“马天奴”；通过后才能执行真实同步。

真实同步读取近 14 天普通订单和当前 SKU 库存，不保存收件人、地址或电话等隐私字段。取消单和拒收单不计入销量；首期退货量仍需要后续接入售后申请单接口复核。

### BNX唯品会单店试跑

页面顶部选择“BNX唯品会”，再进入“API 配置与试连”。BNX 的应用凭证、店铺身份、商品、销售、库存、同步记录和补货计划全部按店铺隔离，不会覆盖马天奴唯品会的数据或计划。

BNX 接口正式鉴权需要三项内容：

- 唯品会开放平台审批得到的 `AppKey`
- 与该应用配套的 `AppSecret`
- BNX 店铺主账号授权该应用后生成的 `AccessToken`

只取得 `AppKey` 和 `AppSecret` 还不能读取店铺数据。三项凭证未齐全时，系统只检查 `https://vop.vipapis.com` 官方生产网关并显示“凭证待配置”；店铺身份校验成功后才开放真实同步。首次同步会读取 BNX 商品/SKU、近 14 天有效订单和当前 SKU 库存，再按现有补货条件生成独立的 `VIP-BNX-*` 计划。

BNX 凭证可在页面加密保存，也可使用独立环境变量提供：

- `VIPSHOP_BNX_ENVIRONMENT=production`
- `VIPSHOP_BNX_APP_KEY`
- `VIPSHOP_BNX_APP_SECRET`
- `VIPSHOP_BNX_ACCESS_TOKEN`

页面保存的 `AppSecret` 和 `AccessToken` 使用本机密钥加密，界面只显示是否已配置，不回显明文。BNX 单店闭环确认前不会启用自动同步频率。

## 天猫店铺工作区与 API 试连

页面顶部按店铺划分独立工作区：

- 马天奴唯品会
- 马天奴天猫官方旗舰店
- BNX唯品会

切换到“马天奴天猫官方旗舰店”后，可进入“API 配置与试连”。天猫凭证、连接状态、数据和补货计划均按店铺隔离，不会混入唯品会批次。第一阶段需要淘宝开放平台应用的 `AppKey`、`AppSecret`，以及马天奴天猫官方旗舰店授权后得到的 `SessionKey`。

`AppSecret` 和 `SessionKey` 使用本机独立密钥加密保存，也可通过 `TMALL_APP_KEY`、`TMALL_APP_SECRET` 和 `TMALL_SESSION_KEY` 环境变量提供。配置前仍可探测 `https://eco.taobao.com/router/rest` 官方生产网关；三项凭证齐全后，系统才会调用 `taobao.shop.seller.get` 校验授权店铺名称。

天猫第一阶段不会直接生成补货计划。只有店铺身份、商品与 SKU、近 14 天销售和在售库存四类只读权限及数据口径全部验证通过后，才会开放同步和计划生成按钮。

## 正式接口接入前置

需要业务侧提供：

1. 唯品会商家开放平台应用及上述 Marketplace 接口授权。
2. 店铺编码和仓库范围。
3. 销售口径：下单、支付或发货，退货计入日期规则。
4. 可售库存、锁定库存、残次品和在途字段映射。
5. 企业微信应用或群机器人地址（如需站外提醒）。
